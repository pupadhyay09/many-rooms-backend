@using Microsoft.AspNetCore.Http;
@using Microsoft.AspNetCore.Mvc;
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@{
    var sideUserRole = HttpContextAccessor.HttpContext.Session.GetString("UserRole");
    TempData.Keep();
    string? currentPage = ViewContext.HttpContext.Request.Path;
}

<style>
</style>
<div class="nav-header">
    @if (sideUserRole == "Demo User")
    {
        <a href="javascript:void(0)" class="brand-logo">
            <div class="logo-abbr">
              @*   <img src="~/images/logo.png" alt="" class="menu-toggle-hide"> *@
                <img src="~/images/logo/Logo_black.png" alt="" class="menu-toggle-show">
            </div>

        </a>
    }
    else
    {
        <a href="@Url.Action("Index","Home")" class="brand-logo">
            <div class="logo-abbr">
                @* <img src="~/images/logo.png" alt="" class="menu-toggle-hide"> *@
                <img src="~/images/logo/Logo_black.png" alt="" class="menu-toggle-show">
            </div>
        </a>
    }

    <div class="nav-control">
        <div class="hamburger">
            <span class="line"></span><span class="line"></span><span class="line"></span>
        </div>
    </div>
</div>

<div class="header">
    <div class="header-content">
        <nav class="navbar navbar-expand">
            <div class="collapse navbar-collapse justify-content-between">
                <div class="header-left">
                    <div class="dashboard_bar">
                    </div>
                </div>

                <ul class="navbar-nav header-right">
                    <li class="nav-item dropdown header-profile">
                        <a class="nav-link" href="#" role="button" data-bs-toggle="dropdown">
                            @* <img src="~/Content/images/images.jpg" width="20" alt="" /> *@
                            <img src="~/images/logo.png" width="20" alt="" />
                           @*  <img src="~/images/logo/Logo_black.png" width="20" alt="" /> *@
                            <div class="header-info">
                                @* <span>@SessionManagement.UserName</span>
                                <small>@SessionManagement.RoleName</small> *@
                                <span>Test</span>
                                <small>Admin</small>
                            </div>
                        </a>
                        <div class="dropdown-menu dropdown-menu-end">
                            <a href="@Url.Action("Logout","Login")" class="dropdown-item ai-icon">
                                <svg id="icon-logout" xmlns="http://www.w3.org/2000/svg" class="text-danger"
                                     width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                     stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                                    <polyline points="16 17 21 12 16 7"></polyline>
                                    <line x1="21" y1="12" x2="9" y2="12"></line>
                                </svg>
                                <span class="ms-2">Logout </span>
                            </a>
                        </div>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
</div>